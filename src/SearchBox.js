import React from 'react';

const SearchBox = () => {
    return (
        <input
            className="pa3 ba b--black"
            type="search"
            placeholder="Search by name"
        />
    );
};

export default SearchBox;
